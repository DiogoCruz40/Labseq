version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: always
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    restart: always
